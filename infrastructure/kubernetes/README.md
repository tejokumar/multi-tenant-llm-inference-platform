# Kubernetes

Kubernetes configuration will define model workers and the platform components that run in GKE Autopilot. Keep base configuration separate from environment-specific values when that distinction becomes necessary.
