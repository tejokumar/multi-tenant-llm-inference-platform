# Terraform

Terraform configuration will create the Google Cloud resources needed by the current milestone. Each module must document its inputs, outputs, cost-sensitive settings, and cleanup command.
