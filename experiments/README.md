# Experiments

Each experiment lives in its own numbered directory. It must include the question, setup, exact commands, expected result, observed result, and cleanup steps.

Raw output that contains credentials, tenant data, or private endpoints must be sanitized before it is saved.
